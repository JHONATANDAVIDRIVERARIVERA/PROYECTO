Flask==3.0.0
Pillow==10.1.0
werkzeug==3.0.1
# Use CPU-only version to reduce memory footprint (200MB vs 400MB+)
# Install tensorflow-cpu first, it will install compatible numpy version
tensorflow-cpu==2.15.0
gunicorn==21.2.0
